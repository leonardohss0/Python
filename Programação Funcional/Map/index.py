#map aplica algum operador em uma lista

L0 = [1, 2, 3, 4, 5]

#incrementar cada elemento da lista L0
map(lambda x: x + 1, L0)

#quadrado dos elementos
map(lambda x: x * x, L0)

#transformar em str
map(lambda x: str(x), L0)
